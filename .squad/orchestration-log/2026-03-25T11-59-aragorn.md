# Orchestration Log: Aragorn (2026-03-25T11:59Z)

**Agent:** Aragorn (Operator)  
**Mode:** background  
**Model:** claude-sonnet-4.6  
**Issue:** #156  
**Branch:** squad/156-kusto-guide

## Task

Research TI production Kusto cluster — tables, schemas, sample queries from past ICMs

## Outcome

**Status:** SUCCESS

## Deliverables

- **File:** `docs/research/kusto-cluster-research.md` (700 lines)
- **Content:** 4 tables documented, 20+ queries validated, MCP Kusto bug documented

## Key Findings

1. **Cluster Structure:** TI production Kusto cluster (`ti-prod-kusto-cluster.northeurope.kusto.windows.net`) has single database (`prod`) with 4 tables: Log, SentinelLogEntry, StixWebApiLogs, TraceEvent. All retain ~7 days of data.

2. **Azure MCP Kusto Tool Broken:** Tool fails with `FileNotFoundException` for all operations (NOT permissions issue). Direct REST API access works. Tool needs configuration repair.

3. **Token Resource Gotcha:** Authentication requires `https://kusto.kusto.windows.net` as token resource—NOT cluster URI. Using cluster URI returns 401.

4. **ARM Queries Not Available:** ICM-embedded queries using `macro-expand ARMProdEG` target ARM production cluster, not TI cluster. Gap for ARM error rate ICM investigation. SAW or DGrep remains only path.

5. **Baseline Error Rates:** STIX API shows constant background failure rate of ~27–32K failures/hour regardless of load volume. Error COUNT is NOT reliable health indicator—error RATE matters.

## Impact

- Aragorn can now use REST API workaround for Kusto queries during ICM investigation
- Bilbo has raw data for polished guide
- Gimli: MCP tool failure may be configuration issue in mcp-config.json
